@model MiniProjetBibliotheque.Models.Domaine

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Domaine</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomDomaine)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomDomaine)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class="col-sm-2">
            Livres correspondants
        </dt>
        <dd class="col-sm-10">
            @foreach (var item in Model.LivresDomaine)
            {
                @Html.DisplayFor(modelItem => item.Titre)

                <br />
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.DomaineId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
