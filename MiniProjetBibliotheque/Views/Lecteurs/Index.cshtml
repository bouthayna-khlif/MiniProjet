@model IEnumerable<MiniProjetBibliotheque.Models.Lecteur>

@{
    ViewData["Title"] = "Index";
}

<h1 class="display-4">Liste des Lecteurs</h1>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Rechercher par Nom ou Prenom: <input type="text" name="SearchString" value="@ViewData["CurrentFilter"]" />
            <input type="submit" value="Rechercher" class="btn btn-dark" /> |
            <a class="btn btn-outline-dark" asp-action="Index">Retour a la Liste complète</a>
        </p>
    </div>
</form>

<p>
    <a class="btn btn-dark" asp-action="Create">Créer un nouveau lecteur</a>
</p>
<table class="table border-dark">
    <thead>
        <tr>
            <th>
                Nom
            </th>
            <th>
                Prénom
            </th>
            <th>
                Email
            </th>
            <th>
                Téléphone
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adresse)
            </th>
            <th>
                Mot de passe
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomLecteur)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrenomLecteur)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailLecteur)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TelephoneLecteur)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adresse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mdp_Lecteur)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.LecteurId"><button type="button" class="btn btn-outline-dark"><i class="bi bi-pen"></i> Éditer</button></a> |
                    <a asp-action="Details" asp-route-id="@item.LecteurId"><button type="button" class="btn btn-outline-dark"><i class="bi bi-info-circle"></i> Détails</button></a> |
                    <a asp-action="Delete" asp-route-id="@item.LecteurId"><button type="button" class="btn btn-outline-danger"><i class="bi bi-x-circle"></i> Supprimer</button></a>
            </td>
        </tr>
}
    </tbody>
</table>
